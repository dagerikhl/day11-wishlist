export interface PersonalizationStrings {
  author: string;
  "check-wish": {
    checked: {
      action: string;
      success: string;
      error: string;
    };
    unchecked: {
      action: string;
      success: string;
      error: string;
    };
  };
  contact: {
    email: string;
    label: string;
  };
  goto: {
    "wish-url": string;
  };
  lead: {
    1: string;
    2: string;
    3: string;
  };
  pools: {
    aquired: {
      "no-wishes": string;
      title: string;
    };
    unaquired: {
      "no-wishes": string;
    };
  };
  title: string;
}
