export interface PersonalizationStrings {
  "aquired-pool": {
    title: string;
  };
  author: string;
  "check-wish": {
    checked: string;
    unchecked: string;
  };
  goto: {
    "wish-url": string;
  };
  lead: {
    1: string;
    2: string;
    3: string;
  };
  title: string;
}
